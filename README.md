# finance_dashboard
A frontend-only personal finance dashboard with summary cards, Chart.js visualizations, sortable &amp; filterable transactions, spending insights, and simulated role-based UI (Viewer/Admin). Built with vanilla JS, HTML &amp; CSS — no framework, no build tools. Data persists via localStorage.
